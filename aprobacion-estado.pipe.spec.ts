import { AprobacionEstadoPipe } from './aprobacion-estado.pipe';

describe('AprobacionEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new AprobacionEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
